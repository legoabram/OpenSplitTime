<% content_for :title do %>
    <% "OpenSplitTime: List organizations" %>
<% end %>

<div class="row header">
  <div class="col-xs-5">
    <h2>
      <strong>Organizations</strong>
    </h2>
  </div>
</div>

<div class="row">
  <div class="col-xs-8">
    <%= paginate(@presenter.organizations) %>
  </div>
</div>

<%= render 'organizations', presenter: @presenter %>

<hr>
<div class="row">
  <div class="col-xs-8">
    <%= paginate(@presenter.organizations) %>
  </div>
</div>
