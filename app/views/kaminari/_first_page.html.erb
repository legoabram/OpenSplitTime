<% if current_page.first? %>
  <li class="disabled">
    <%= content_tag 'a', raw(t 'views.pagination.first') %>
  </li>
<% else %>
  <li>
    <%= link_to raw(t 'views.pagination.first'), url, remote: remote %>
  </li>
<% end %>
